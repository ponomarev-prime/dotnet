FROM ubuntu:22.04

RUN apt-get update && apt-get install -y \
curl \
wget \
build-essential \
unzip \
libicu-dev

COPY ./dotnet-sdk /dotnet-sdk
COPY ./scr-my /scr-my
COPY ./proj_x /proj_x

RUN /scr-my/export4bash.sh

WORKDIR /proj_x

EXPOSE 8020